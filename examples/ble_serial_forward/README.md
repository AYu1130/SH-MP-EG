# 示例：BLE 串口透传转发器

`forward.py` 演示一种 **调试技巧**：在 PC 端用 bleak 连接 HM-10
（或任何 NRF UART / BT05 兼容模组），把 BLE 收到的字节打印到串口风格，
同时把 stdin 输入当作下行写回。

场景：
- STM32 还没调好 BLE 固件，先用手机 / PC 模拟与网关通信；
- 排查 BLE 通道的字节顺序 / 粘包问题；
- 记录 HM-10 透传数据做离线分析。
